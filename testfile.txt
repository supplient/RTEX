let Matrix M_{10, 20};
M_{i,j}=2*i+cos(j) #where i=1,2...10 #where j=1,2...20;
let Matrix G_{10};
G=M*M^T;

let Real k;
k = M_{0, 1};
let Real c;
c = (
    3, if k==2 #
    1, else
);
M_{i,j} = (
    2, if M_{i,j}>3 #
    1, else
    ) 
    #where i=1,2...10
    #where j=1,2...20
;


let Matrix C_{2};
C = [
    2, 3,
    1, 2
];

let Real s;
s = \sum_{i=1}^{2} \sum_{j=1}^{2} C_{i, j};